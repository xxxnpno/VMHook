# vmhook Test-Coverage Ledger — gate 2 (exhaustive tests, every input)

Generated 2026-06-24 from the 7-cluster read-only coverage-map wave (run wf_84dd7728).
Measurable gate-2 checklist: per feature, input dimensions covered vs OPEN, and whether
closing the gap is no-JVM-closable or capped by the #38 no-SEH safepoint stall.

## Aggregate
- **106 features** inventoried; **avg coverage ~78%**.
- lane: **19 no-JVM**, **30 JVM**, **57 both**.
- **61 of 106** features have remaining gaps **gated by #38** — the dominant blocker to 100%.
- bands: **18** >=90%, **82** 50-89%, **6** <50%.
- **496 open gaps** enumerated total.

## Actionable NOW — no-JVM-closable open gaps (not #38-blocked), lowest coverage first

- **dllmain_bootstrap** (8%, both): NO dedicated test file exists (no tests/test_dllmain_bootstrap.cpp, no jvm/modules/dllmain_bootstrap.cpp); no-JVM: once_flag/call_once idempotency under single-threaded re-entry (exactly 1 spawn); no-JVM: thundering-herd race (hardware_concurrency threads) -> exactly 1 spawn, under TSan; no-JVM: throwing-callable call_once re-arm behavior + detach cannot throw past guard; no-JVM: resettable-flag re-init produces 2nd worker (locks in flaw #1 TODO); JVM: get_instance() non-null (single point of failure / readiness gate); JVM: all 13 baseline register_class re-call -> true; find_class round-trips each name incl nested $ + java/lang/System; JVM: worker runs on the detached worker thread-id (not loader); JVM: registry count >= module-file count (half-link catch); readiness-gate-timeout / DLL_PROCESS_DETACH (no detach handling) negative cases
- **hw_breakpoint_dr7** (40%, no-JVM): full Cartesian sweep (4 slots x 2 kinds x 4 lengths = 32) each vs independently recomputed expected - only ~5 of 32 spot-checked; R/W and LEN field placement isolated per slot for slots 1 and 2; explicit counter-intuitive eight_bytes->0b10 / four_bytes->0b11 LEN assertion guarding against a tidy-up; field-disjointness / no-stray-bit (dr7 == exact expected mask, incl upper 32); two-slot OR-composability ((build_dr7(0,..) & build_dr7(3,..))==0); cross-check vs refresh_thread_drs merge mask (flaw #2: build_dr7(slot,..) & ~merge_mask == 0); boundary/defensive build_dr7(-1/4/INT_MAX) (UB today - document until guarded); constexpr-ability static_asserts on the 32 results; sizeof->LEN selection ladder table incl odd-size->eight_bytes coercion
- **os_protect** (58%, no-JVM): old_prot success-write semantics per platform (Win real PAGE_*, POSIX exactly 0); round-trip restore using returned old_prot (pin POSIX-unsafe); out-of-range enum fallback -> no_access + returns true; SIZE_MAX / address-space-wrapping size -> false (drives overflow fix); no_access + execute_rw behavioural probe via safe_read asymmetry; idempotent re-protect twice; size==page exact vs size==page-1 rounding boundary pair; old_prot==nullptr on SUCCESS path; alignment witness: sub-page protect does not bleed into neighbour page writability
- **os_allocate_release** (62%, no-JVM): multi-page + granularity-sized + sub-page allocations writable end-to-end; 1000-iter alloc/write/release leak/stability loop; negative release containment (unaligned/interior ptr no-crash, pin silent no-diagnostic); hint honoured-or-ignored never returns occupied addr; returned pointer page-alignment assertion; execute-bit positive check (write 0xC3, call through fn-ptr, !Apple); DWORD->size_t cast non-truncation pin; sysconf 4096-fallback architecturally-valid-page-size pin
- **object_factory** (70%, no-JVM): make_unique<T>(args...) with constructor arguments not exercised (only zero-arg); factory under a multi-level wrapper hierarchy not instantiated; no live-JVM proof the factory actually builds a usable wrapper end-to-end (that lives in register_class JVM module, not here); no JVM module dedicated to object_factory specifically; no agent spec exists for object_factory (object_factory-specialist.md absent)
- **os_query_region** (72%, no-JVM): Windows PAGE_GUARD guarded==true + is_readable_pointer false (flaw #4) gated #if WINDOWS; deliberate unmapped-hole free reporting (reserve 2, free middle); macOS hole-aliasing wrong-region (flaw #2) — platform, no CI mac runner; macOS/iOS never-set-free trampoline gap (flaw #1) — platform; MEM_RESERVE committed=false&&free=false reserved-region case (flaw #5)
- **logging_format** (72%, no-JVM): active vs no-op divergence via rdbuf capture (active emits exact output, no-op emits nothing + sizeof unevaluated); thread-safety K-threads x M-lines no torn << count exact; std::vformat boundary widths INT_MIN/MAX, LLONG, UINT, {:+}/{: }/{:#x}/{:#o}/{:#b}; float specials 0.0/-0.0/inf/nan/subnormal/{:.17g}/{:e}/{:f}/{:a}, char vs signed/unsigned, bool {:d}; unicode/multibyte/embedded-NUL/control bytes through format_log; empty fmt with args, single { / } unmatched-brace -> throw -> catch verbatim; argument-count mismatch matrix, out-of-range positional {5}; info_tag/warning_tag .front()=='[' symmetry
- **jni_forwarders** (72%, no-JVM): no agent spec exists (jni_forwarders-specialist.md absent) - intended-angle list unverified; no live-JVM module proving each forwarder actually forwards to the real JNI fn-table slot (only type-identity + no-JVM null contract); JNI function-table slot-index correctness (slots 6/21/22/23 etc.) not asserted against a live JNIEnv; oop_handle out-param mutation path only type-probed
- **signature_parsing** (75%, both): signatures ending in ) ('()','(I)','(Lstr;)') rparen+1==size boundary (flaw #2 fix now present — pin it); unknown-return policy explicit pin return_basic_type_of('(I)Q') (flaw #1 — currently T_OBJECT vs JVM-path T_VOID divergence); sig_char_to_basic_type total 0..255 byte sweep (signed-char high-byte -> default); jvm_primitive_byte_width adversarial widths ' I'/'I '/NUL-view/'\xFF'; jni_signature_for_arg integral-width matrix long/long long/size_t/ptrdiff_t/wchar_t/char16_t/char32_t per platform; enum class/scoped-enum hit static_assert not is_integral; class-map descriptor build registered vs unregistered Object fallback (flaw #5); full ctor-signature build '(IDLjava/lang/String;)V' + empty '()V' + uint16->'(C)V'; signature_for_arg == jni_signature_for_arg parity
- **hook_signature** (78%, JVM): no superclass/inherited-overload walk (flaw #3) - inherited overload throws Method-not-found, not pinned; wrapper-detour arity/width vs descriptor mismatch undiagnosed (flaw #5) not exercised; expected_signature drift falling back to name-only after redefinition (flaw #6) needs JVMTI RedefineClasses (unreachable in-process); mid-install failure leaking NO_COMPILE + dead entry (flaw #1) not regression-pinned; compressed-oops-OFF lane for the reference-overload String/Object decode
- **hook_basic** (80%, both): half-installed-method re-install poisoning (flaw #1) not regression-pinned on a JVM; push_back-realloc UAF while sibling detour iterates (flaw #2) not exercised (stays inside install-before-fire contract); compressed-oops-OFF lane: self/String decode via the bits<=0xFFFFFFFF heuristic never run with -XX:-UseCompressedOops; 0-arg instance method (slot 0 == this only) not driven; _dont_inline silent no-op when Method._flags VMStruct absent (flaw #4) not characterized live; all single-slot primitive widths (byte/short/char/float) at boundary values through the typed callback only spot-checked
- **hook_chaining** (80%, JVM): lock-free common_detour iteration vs reallocating push_back of a NEW shared-stub method mid-flight (flaw #1 UAF) deliberately not exercised; Method*-identity mis-route after JVMTI RedefineClasses recycles an address (flaw #2) needs a second agent - unreachable in-process; cross-DLL chain_resume path (needs a 2nd patching DLL) not testable single-process; O(N) linear-scan scaling characteristic with many hooked methods not measured; compressed-oops-OFF lane for the self/String detours
- **os_safe_read** (80%, no-JVM): dst-state-on-false pinned (partial process_vm_readv torn data, flaw #2) — char. only; handler SIGBUS self-disarm asymmetry (flaw #3) — library, needs live signal; handler chaining to HotSpot's prior SIGSEGV/SIGBUS (flaw #4) — JVM-only; iOS always-true contract violation (flaw #1) — platform, untestable in CI
- **traits_function_traits** (80%, no-JVM): const-ref vs by-value param spelling identical offsets + extract_frame_arg collapse (flaw #2); empty tuple_tail SFINAE detectability (member absent today, flaw #1); generic/overloaded/templated operator() falls through (member absent, flaw #3); noexcept / ref-qualified operator() (member absent, flaw #4); all-widths offset matrix (bool,long,char16_t,double,int8,int)->{0,1,3,4,6,7}, begins/ends with J/D; self-less static shape (return_value&,int,long,int)->(int,long,int) {0,1,3}; 8-arg fold accumulation past small cases; tuple_element_t round-trip per k
- **platform_capability_macros** (82%, no-JVM): aggregate normalisation (VMHOOK_OS_POSIX exactly 0/1, flaw #1); compiler-macro invariants MSVC/CLANG/GCC each 0/1, clang-cl edge (flaw #5); full LEN/kind ordinal pin two_bytes==0b01 four_bytes==0b11 (flaw #6); build_dr7 exhaustive 4 slots x 2 kinds x 4 lengths = 32 masks + no enable-bit collision; DR7 bit-field non-overlap per slot + cross-slot 4*slot stride; user_address_ceiling as tag-strip mask on synthetic tagged pointers; POSIX granularity==page_size exact equality; memory_protection 5 values pairwise distinct + region_info trivially default-constructible
- **register_class** (82%, both): the insert_or_assign vs emplace asymmetry is a characterized-but-unfixed library bug (collide_a/collide_b); concurrent register_class race under registration_mutex not stress-tested live; re-registration after shutdown_hooks teardown not covered (was Wave-3 cascade crasher)
- **api_surface_no_jvm** (82%, no-JVM): make_unique<T>(args...) with ctor args still uncovered (only zero-arg); full never-throw blanket over ALL ~30-40 public entry points - spec says ~12 still unaudited that way; call-order-permutation independence (find_class/register_class/hook orderings) not asserted; type-surface breadth: inherited multi-level object<> hierarchy + non-trivial-dtor wrapper compile-instantiation per compiler; for_each_* second-call negative-cache stability
- **hook_unhook_double_free** (84%, JVM): half-installed poisons re-install (flaw #2) not regression-pinned; _dont_inline half-clear when Method._flags VMStruct absent but access-flags fine (flaw #4) JDK-variance not exercised; in-flight-callback race on stop() (flaw #5 UAF) deliberately not exercised (driver-thread-only contract); _code/entry-points intentionally-not-restored (flaw #3) only behaviourally proven, not structurally inspected; compressed-oops-OFF lane for the self seed-check
- **watch_static_field** (85%, JVM): Windows-x86_64-only feature: the live-trap angles only run on one CI platform leg (off-Windows asserts only the empty-handle fallback); odd-sized field_type (3/16 bytes / struct) coercing to 8-byte window + alignment requirement not exercised; unaligned-slot relax-to->=1-fire path is an [INFO] fallback, weakening the once-per-write assertion in that case; compressed-oops-OFF interaction with the watched static field address (default-layout only)
- **wrapper_pattern** (85%, both): multi-level inherited object<> hierarchy compile-instantiation breadth (libstdc++ vs libc++ unique_ptr static_assert) only partial; wrapper with non-trivial destructor not instantiated across all compilers; field_introspection SECTION-H GC-internal probe gated off on msvc.JDK11+
- **shutdown_hooks_teardown** (86%, JVM): watchdog non-respawn after teardown (the historical regression) only pinned via the re-fire beat, not via direct g_started inspection here; partial-restore (_code/entry-points left deopted) only behaviourally proven; shutdown racing an in-flight detour (the 5972 early-return window) not exercised
- **collection_iteration_safety** (86%, both): -XX:-UseCompressedOops config: populated shapes can decode 0 ([INFO]-gated + majority floor tolerate it, never HARD-verified); 1<<20 / 1<<24 / linked-list size runaway caps never hit (largest 5000) — cap-saturation untested; concurrent-mutation torn-read during a walk (no live mutator-thread test); fabricated-array walk for iterate_struct_entries match-found / field_name==nullptr skip path (no-JVM cannot drive; needs synthetic array)
- **unified_call_syntax** (86%, no-JVM): pure compile-time/SFINAE only - no runtime value-passing proof here (that lives in method_call_* JVM modules); pointer/enum/user-defined non-wrapper arg-type rejection breadth; no-arg and >2-arg method accessor probes thinner than 0/1/2-arg
- **hook_reinstall_after_shutdown** (88%, JVM): watchdog NOT stopped by scoped_hook drop (flaw #1 by-design) only pinned indirectly via final shutdown; wait_for_exit() bounded-wait spawning a second watchdog (flaw #2) not deterministically reproducible/unasserted; -DVMHOOK_DISABLE_AUTO_REPAIR build leg only conditionally compiled, not a routine CI lane
- **method_explicit_signature** (88%, both): instance get_method(name,sig) phantom-this for a STATIC method (flaw #1) sidestepped via static_method, never driven through call(); empty-sig strict-miss vs hook<T> wildcard divergence only pinned, not reconciled; get_method_by_oop_klass parity gap (no sig-aware sibling) unexercised; noexcept/bad_alloc-under-pressure contract claim untested
- **collection_list** (88%, both): -XX:-UseCompressedOops / >32GB-heap decode path (all narrow-oop reads silently empty) — never run in CI; size > elementData.length pathological clamp (only size<capacity exercised, not the OOB-tail direction live); LinkedList cycle/over-long chain with corrupt next on a live JVM (bounded only by size; cannot forge safely); empty-vs-decode-failure ambiguity not distinguishable by a field caller (characterized, not closed)
- **return_set_arg** (88%, both): primitive branch missing sign-extension (flaw #2) is only characterized, not yet asserted as -1 (awaiting lib fix); off-by-one at max_locals ceiling: index==65535 admitted (flaw #3) deliberately NOT executed on a live frame (would corrupt); NewStringUTF OOM leaves pending exception (flaw #4) not exercised (cannot force OOM safely); make_java_string 4096-char truncation parity vs JNI fast path only probed via 5000-char [INFO], not the truncation boundary itself
- **version_macros** (88%, no-JVM): field-carry boundary MAKE(0,0,1000)==MAKE(0,1,0), MAKE(0,1000,0)==MAKE(1,0,0) (flaw #2 failure-mode spec); int-overflow ceiling MAKE(2147,0,0) representable / 2148 overflows (flaw #1); macro-hygiene 2*MAKE(0,0,1)==2, MAKE+1==2, inside real #if; #if preprocessor-context usability with #else #error; stringize value-vs-name regression lock (no 'VMHOOK'/alpha char, flaw #5); VERSION_STRING<->packed component-level agreement via reparse; negative CMake-sync guard (defs actually present); idempotence/multiple-inclusion two-TU
- **collection_map** (89%, both): -XX:-UseCompressedOops / >32GB-heap (every key/value/next/left/right read silently empty; header offsets +12/+16 wrong) — never run in CI; LinkedHashMap insertion/access-order NOT asserted (bucket order pinned deliberately; the actual contract untested); name-based table-shadows-root tie-break only latent (no live Map exposing both); concurrent rehash/rotation torn-read (caps bound loops, not torn reads) — quiescent fixture only
- **field_null_safety** (90%, both): null-safety asymmetry under a forced GC (stale wrapper) on the live JVM; embedded-NUL / overlong name behaviour on JDK21+ FieldInfoStream symbol-compare path explicitly; null array-element decode (get_array_element on a null array oop) null-contract
- **find_methods_by_signature** (90%, both): no descriptor validation log (flaw#1) only pinned as silent-empty, not fixed; silent under-report on bad Method* slot not driven (needs corrupt VM); O(n) re-walk + per-name alloc (no caching) is perf not correctness; modified-UTF-8 descriptor referencing a Unicode class name untested
- **collection_linked_list** (90%, both): -XX:-UseCompressedOops / no-compressed-class-pointers (narrow oop + narrow klass@+8 assumed) — never run in CI; unsynchronised size-vs-chain race (mutator concurrently mutating during walk) — only quiescent fixture; find_field miss not memoized -> elementData super-walk per read is perf-only, not asserted; >4096-char element string truncation in read_java_string (only short elements used)
- **collection_set** (90%, both): -XX:-UseCompressedOops / >32GB-heap (all node/array narrow-oop reads decode garbage) — never run in CI; LinkedHashSet insertion-order contract untested (only order-independent content); 1<<20 per-bucket / 1<<24 total runaway caps never approached (largest is 5000); no in-walk count-vs-Set.size() cross-check inside the library (test cross-checks; library does not)
- **collection_hash_tree_map** (90%, both): -XX:-UseCompressedOops / >32GB-heap (whole table/root walk silently empty; +8 klass / +12 length / +16 data offsets wrong) — never run in CI; LinkedHashMap insertion/access-order assertion still absent (bucket order only); TreeMap left-spine inner-loop has no iteration cap (corrupt left-cycle OOM) — cannot forge live; table-shadows-root tie-break only latent
- **return_value_cancel** (90%, both): tier-up-then-cancel race (compiled/inlined caller makes cancel a silent no-op) - flaw #2, structurally not exercised; non-x64 / !VMHOOK_RUNTIME_HOOKING_AVAILABLE path where installed()==false and cancel is a no-op; descriptor-blind dual rax+xmm0 store on a c2i resume path (flaw #1) - latent, no live trigger; null return_slot in cancel() AV (flaw #3) - contract-only, no test asserts the no-guard hazard
- **os_page_size_granularity** (90%, no-JVM): sysconf non-power-of-two hostile-libc path (flaw #1) — theoretical, library assert; granularity-smaller-than-page invariant clamp (flaw #2) — library
- **field_primitives_get** (92%, JVM): compressed-oops-OFF (non-default heap) primitive-vs-ref offset layout (spec marks out of scope); reading a primitive via an explicitly wrong-width descriptor through the real slot (wrong-sig-over-real-pointer is only in null_safety, not the live primitives_get path)
- **field_primitives_set** (92%, JVM): signaling-NaN/payload write survival THROUGH Java bytecode (spec: native->field->native only, JDK-canonicalization out of scope); reference-write parity guard gap (no width guard on ref writes) is documented not exercised here — owned by field_static/object_ref; spatial-adjacency anti-clobber proof delegated to field_set_size_guard
- **collection_type_tags** (92%, no-JVM): live-JVM exercise of the array-branch fix (ec1c8a8) on a real Object[] field (count + null-slot) — currently only collection_list does the raw '[L' walk, no type-tag-owned JVM module; Flaw#1 to_vector-on-Map-field silent wrong-path (get(int) on Map.get(Object)) not characterized live; Flaw#2 generic get(int) fallback on non-RandomAccess/Queue/Deque O(N^2) impls not exercised
- **method_is_reference** (93%, both): find()-vs-rfind() dual-parser divergence on a two-) descriptor not cross-checked (unreachable from real metadata); name-only-proxy signature()/is_reference describing a different overload than a later call() dispatches (sidestepped by always pinning); no companion is_object/is_array so [I semantic ambiguity only documented
- **return_set_primitives** (93%, both): set() on a method whose Java return descriptor mismatches the C++ type (flaw #3, no BasicType check) only characterized via over-wide masking, not wrong-kind (e.g. double into int method); plain char signedness divergence (flaw #1) defended by avoidance, never directly asserted on the JVM; >8-byte / long double rejection (flaw #2) is a compile error, not exercised; compiled-caller bypass of the i2i force-return path (interpreter-only scope) not exercised
- **field_proxy_value_t** (95%, no-JVM): decoding a NON-zero compressed OOP through value_t (void*/string/unique_ptr) needs a live heap - JVM-only, intentionally out of scope; populated vector<T> / unique_ptr<W> success arm over a real array/oop (JVM-only); value_t variant alternative ordering re-asserted if vmhook.hpp variant changes (mirrored constant, not derived)
- **field_proxy_set_guards** (95%, no-JVM): actual primitive/string/array/unique_ptr write LANDING in a real Java field (success path) - JVM-only via field_set_size_guard/field_static siblings; anti-clobber adjacency + Java getfield read-back (JVM-only); value_t->vector<T> end-to-end on a live array (JVM-only); byte-order assumption is host-native both ways - real JVM field byte order is the sibling module's boundary
- **method_proxy_value_t** (95%, no-JVM): live producers (call/call_jni/call_stub actually storing each alternative from a real Java return) belong to the JVM call modules, not here; get_compressed_oop 4-byte memcpy read via a stack uint32 address only partially covered; real unique_ptr arm wrapping a live oop (needs JVM) out of scope
- **method_call_arg_cap** (95%, no-JVM): over-cap direction intentionally NOT detectable (a failing static_assert is a hard error, not SFINAE) — by design, no live counterpart; the runtime param_idx>=8 silent-drop guard on the call_stub path has no live exercise (stub absent on CI; and over-cap can't be expressed in valid C++)

## #38-GATED — gaps that need the safepoint stall solved first
Honest: these cannot reach 100% by adding tests; they need the #38 fix (live raw-VM-walk depth).

- **seh_invoke_detour** (38%, both): NO live-JVM module: hardware-AV containment (null-receiver/stale-OOP/wild-pointer/extract_frame_arg fault) - the MSVC __except arm's whole reason - is unproven by any test; false-return allow-through (fault -> original body runs, JVM survives, subsequent clean call fires) not exercised; cross-toolchain C++-throw dtor asymmetry (MSVC skips ~object vs MinGW runs it) not made observable; static-method detour fault containment; MinGW/GCC/Clang expected-to-abort hardware-fault gating not exercised (flaw #2: non-MSVC has no SIGSEGV containment); /EHa regression sentinel (flaw #1: build never sets /EHa) absent
- **hook_common_detour_dispatch** (45%, both): NO dedicated JVM module: dispatch CONTRACT (scan semantics, fire-once structure, SEH isolation, thread-guard restore, no-match path) has no owner module; SEH firewall isolates a faulting detour (the single most valuable missing JVM test) - process survives AV, original body runs, subsequent clean call fires; two-METHOD discrimination (hook A, call A and unhooked B many times, fires only for A) - distinct from hook_basic's two-INSTANCE test; re-entrancy/nesting: hooked method whose allow-through body calls another hooked method, TLS guard restores across nested return; no-match path skips set_thread_state normalization (flaw #3) - needs Method* drift, unreachable without RedefineClasses; shutdown-bail-wins-the-race-window best-effort assertion; frame_pointer dereferenced un-validated outside SEH (flaw #1) not exercised live
- **os_signal_handler** (45%, both): live-JVM detour AV-containment (null/stale-OOP receiver deref -> allow-through, JVM survives N calls) — MSVC-only firewall, no-SEH on mingw/clang; per-call isolation alternating good/faulting calls on live JVM; multi-thread dispatch hammering hooked faulting method; EXCEPTION_EXECUTE_HANDLER too-broad: stack-overflow/divide-by-zero NOT swallowed (flaw #3) needs cl.exe SEH; handler chaining + SIGBUS self-disarm regression (flaws #2/#3) needs live signal/JVM
- **constantpool_access** (48%, both): live round-trip get_name/get_signature for spread of index values (no-arg/(I)/(JD)/(String,Object[])/init/clinit); live field find_field round-trip primitive/String/array/static/@Contended; get_length()>0 and > largest used index (bound satisfiable not vacuous); index==cp_length and >cp_length -> nullptr not crash on live pool; get_base() non-null, >cp, base[name_index]==get_name symbol; out-of-range index -> recoverable nullptr never AV (field-stream path flaws #1/#2 measured separately); index==0 1-based contract pin (flaw #6); _pool_holder back-pointer class-name recovery instance+static; unicode/over-long symbol cp slot, post-GC decommitted-page read (flaw #5)
- **midi2i_trampoline_alloc** (50%, both): NO live midi2i_hook constructed against a synthetic RWX stub: construct/patch/verify_and_repair/rewrite_chain_resume/destructor-restore only validated as offset arithmetic, never EXECUTED (spec plan-C not done); destructor restore-guard mis-fire under chaining (flaw #4: foreign 0xE9 clobbered by stale original bytes) - the test that fails today - absent; verify_and_repair intact/re-apply/re-chain paths not driven on a real trampoline; chain_resume out-of-32-bit-range resume rel32 overflow (flaw - no range check) not confirmed; live-JVM: one-trampoline-serves-N reuse / survives-handle-drop / shutdown-restores-stub / watchdog-touches-not-realloc / allow-through-vs-cancel through real trampoline (incl movq xmm0 for float/double) - none have a JVM module; cross-DLL chain_resume integration (needs 2nd DLL) documented-only
- **vmstructs_offset_resolution** (50%, both): success path entirely untested: populated synthetic table -> non-null entry, ->offset full uint64 range round-trip, ->is_static surfaced (flaw #1) — needs a free-function seam, no-JVM; terminator/malformed-table robustness (null type_name mid-array, interior null field_name then real match, last-before-terminator); no-match exhaustion on fully-populated array; case-sensitivity/near-miss/prefix (Method vs method, _code vs _code_entry); unicode/embedded-NUL search key; live-JVM end-to-end: real offsets land on right field, static-address readers deref plausible pointer, JDK-matrix renames
- **decode_oop_and_pointers** (55%, both): untag_pointer direct coverage (null, low-46 unchanged, tagged mask-back-exact, mask==ceiling, kernel-half hazard) — no-JVM; safe_read_pointer low-level contract (null/floor/ceiling/8-byte-misalign-before-OS-read/mapped-slot value/unmapped no-fault) — no-JVM; read_pointer<T> zero-extension (high half zero, top-bit no sign-extend) — no-JVM; all-nine poison values in one parity-agnostic loop (closes doc-vs-code flaw #1) — no-JVM; is_readable_pointer 2-vs-8 alignment mismatch pin (flaw #2) — no-JVM; live decode->encode->decode identity across non-zero base + shift 0/3; decode of live receiver oop matches interpreter frame; tagged dictionary entry survives untag+safe_read
- **compressed_klass_decode** (60%, both): live non-zero round-trip: read real header narrow, decode->K, encode(K)==original narrow, decode(encode(K))==K; two header readers agree klass_from_oop==klass_from_object_header (flaw #4); distinct same-class instances -> same klass; different classes -> different (no collision); for_each_instance decode path finds known instances + zero false-positives (flaw #1); klass-vs-oop base/shift produce different pointers on heap where bases differ; compact-object-headers +8 offset break (flaw #2) on Lilliput JDK; -XX:-UseCompressedClassPointers 64-bit slot case (flaw #3)
- **jni_arg_packing** (62%, both): sub-width signature/packer consistency paired invariant (jni_signature B/S/C + packs to .i + low-byte readback, flaw #1); 8-byte non-intN_t types long/size_t/ptrdiff_t compile-asymmetry (flaw #2); uint64/int64 both -> J; char/char16_t/char32_t/wchar_t reach .i (flaw #3) — not exercised; object arm with null unique_ptr (storage==null, value.l==&storage); make_jni_args object args interleaved among primitives+strings, .l stable across reserve; empty string std::string{}/string_view{}/'' tag false + .l null; bool from non-{0,1} byte canonicalisation; live: sub-int arms dispatched to m(byte,short,char,int) with sign-edge values, Java witness fields; live: object/unique_ptr arm via jni_make_unique with null wrapper arg; live: embedded-NUL + BMP/supplementary string truncation/modified-UTF-8 characterization (flaws #4/#5); live: float-vs-double + bool arm dispatch witness
- **decode_u5_unsigned5** (65%, both): non-zero stream_pos mid-buffer entry; boundary byte 192 vs 191 as first AND middle byte; position-4 high-bit sweep pinning 32-bit truncation (flaw #3); numeric-max five-255 = 4278124286 != ~0u sentinel (flaw #4); every low byte 1..191 single-byte loop b->b-1; header+N-field-record grammar with flag-gated optionals against byte buffer; live JDK21+ find_field_in_stream real field offset vs legacy Array<u2> cross-format equivalence; optional-present fields (static-final 0x01 / generic-sig 0x04 / @Contended 0x10) cursor alignment; truncated/corrupt stream fuzz (flaws #1/#2 over-read, unvalidated offset)
- **interpreter_frame_walk** (68%, both): caller() on a non-interpreted immediate caller (flaw #1: no stack_trace-style guards, likely AV) - the headline missing JVM reproducer; get_locals() JDK-21+ index path with >4096 frame-words hitting the >=0x1000 silent-nullptr cliff (flaw #3); all single-slot primitive widths (byte/short/char/bool/float) at min/max/-1/0 through the READ path (only int/long/double spot-checked); sign/representation edges: bool from non-{0,1} slot, char@0xFFFF, short@-1 sign-extend, float NaN/inf, long/double with zero high half; compressed-oops-OFF run (flaw #4: bits<=0xFFFFFFFF ambiguity) - no -XX:-UseCompressedOops lane exists; 0-arg instance + near-max-arity frames on java_slot_offsets extremes; locals_offset mixed-stub last-writer-wins robustness (flaw #2)
- **const_method_bounds** (68%, both): live <init>/<clinit> CP-index decode; every-method-on-class iterate (no crash, no false-reject across whole pool); large constant pool with valid indices > 256; corrupt-index simulation on writable fake ConstMethod (_name_index=cp_length/0xFFFF/0 -> nullptr, process survives); unmapped-pool simulation _constants -> unmapped addr; allow-through unaffected (hook still resolves+fires); sibling field-symbol readers missing FIX B guard (flaw #1) on JDK8 Array<u2> + JDK21 stream
- **method_entry_points_i2i_i2c** (70%, JVM): validate_adapter_handler_entry _i2c_entry-at-offset-0 assumption (flaw #2) - the rejects-garbage pure-logic unit not present; detect_adapter_offset_from_method wrong-slot-that-validates (flaw #3) brute-scan tail not exercised; non-atomic entry-point store data-race window (flaw #5) inherently untestable single-thread; re-anchor (Method freed/aliased) entry restore needs JVMTI RedefineClasses - only the watchdog mode-3 path covers the set_from_interpreted_entry(i2i) restore; c2i recovery degrades to [INFO] when JIT doesn't warm - not HARD on every runner; deeper warm-JIT raw-readback assertions risk the #38 stall on no-SEH cells
- **compressed_oops_decode** (70%, both): encode below-base maps sub-base ptr to null (flaw #1) observable on non-zero-base live heap; encode uint32 truncation + shift-residue alignment (flaw #2) characterization; live shift==0 vs shift==3 + base==0 vs base!=0 regimes under controlled -Xmx / -XX:-UseCompressedOops; two distinct live objects -> two distinct narrow oops, decode recovers each; cross-check decode vs higher-level typed wrapper/field-proxy reader agreement
- **deoptimize_methods** (72%, JVM): JIT-warm is timing/JDK-gated: when _code never becomes non-null the deopt-clears-code core degrades to [INFO], so the headline assertion is not HARD on every runner; this module is a documented #38 STALL TRIGGER on the no-SEH cells (full-graph klass walk under GC pressure) - deeper deopt assertions risk re-tripping; array-klass get_super() JDK-variant garbage-walk edge only walked, deeper per-array-klass assertions not added (perf-sensitive on for_each path); c2i-unrecoverable skipped_no_c2i sweep path only [INFO]
- **method_call_object** (72%, both): JDK21+ call_jni truncated-freed-handle for non-String Object only [INFO], never hard-asserted (the headline live bug); usable-wrapper contract (read field + call method through returned wrapper) hard only on call_stub path (absent on CI); -XX:-UseCompressedOops lossy encode_oop_pointer round-trip untested; missing-registration silent-swallow path not pinned
- **hook_install_after_jit** (75%, JVM): c2i-unrecoverable forced-deopt fallback (fie=i2i, code=null only) only INFO-characterized, not hard-asserted; deeper warm-then-install raw entry-point readback under GC pressure (the angles that re-trip #38); compressed-oops-OFF lane for the self decode in the post-deopt detour; instance vs static c2i-adapter recovery divergence across JDK 8 vs 9+ not isolated
- **hook_verify_repair** (76%, both): modes 1/2 (Method* freed / aliased) only no-throw/intact-characterized; require hostile JVMTI RedefineClasses (out of reach); mode-3 re-fire is i2i-vs-JIT gated best-effort (flake-prone), not HARD on every JDK; deeper live raw-VM-walk verify assertions on the no-SEH cells (the #38-sensitive deepening); watchdog-dead-after-shutdown regression only indirectly pinned
- **method_overload** (78%, both): static-overload resolution DEAD bug (resolve bails object==nullptr) only [INFO], never failed on JDK21; first-match-wins ambiguity for unregistered-wrapper wildcard only characterized; call_stub-path overload re-resolution never the live path on CI; object-returning overload read-back via unique_ptr not exercised (only primitives+String)
- **klass_introspection** (78%, both): array-klass/non-InstanceKlass _methods read returns empty no-crash (flaw #1) — partially via shapes; inheritance-depth 3-level: only-Leaf-declared + hand-walk get_super each level; generics/bridge synthetic compareTo(Object) alongside compareTo(Foo); long/unicode symbol approaching u2 limit -> to_string cap path (flaw #3); get_methods_count vs enumeration-length identity; stale-cache/redefinition re-resolve (find_class stale-guard); concurrency: hammer get_class_methods<T> under class loads
- **make_java_string** (78%, both): JDK8 classic char[] path yields no usable String (flaw #2) still only [INFO]-characterized, not HARD; big-endian StringUTF16 byte order untested (LE-only CI, flaw #4); -XX:-UseCompressedOops / >32GB heap layout shift (+16/+12 offsets) unexercised; survive-GC store-barrier probe gated off on all Windows toolchains; VMStruct field rename drift not negatively probed
- **jni_local_ref_hygiene** (78%, JVM): no no-JVM lane (inherently needs live local-ref table overflow symptom); iteration count bounded to a few hundred - a slow leak (1 ref per N calls) below threshold would not surface; PushLocalFrame/PopLocalFrame framing not directly asserted; concurrent multi-thread detour ref-table interaction untested
- **find_class_context_loader** (78%, both): multi-classloader isolation (same name in two app loaders) live disambiguation; freshly-attached cold-thread JDK17+ resolver path needs deeper live assertion; Forge LaunchWrapper arm only reachable in a real Forge env - unexercised
- **method_call_jni_fallback** (80%, JVM): local-ref-table-exhaustion failure mode only inferred from stability, never forced to exhaustion; DeleteLocalRef-on-primitive-cell aliasing only indirectly proven; Call(Static)?<T>MethodA slot-table correctness covered only by value, no direct slot-index assertion; no_JVM cap mirror exists (arg_cap) but the JNI marshalling itself has no no-JVM unit
- **method_static_portability** (80%, JVM): MSVC/Clang ABI cells only validated locally (.localci) not exhaustively per-compiler in the matrix for every return shape; pool_holder static-klass-derivation edge (VMStruct missing) fail-closed not driven; object usability recorded [INFO] (call-path dependent), never hard on CI
- **method_flags_width** (80%, both): live adjacent-byte anti-clobber on the u1 (JDK8-12) path — existing JVM no-clobber compares _flags to itself (same wrong width), cannot catch a write spilling to offset+1; observed-readback tied to exported type_string not yet asserted live (only empirical latch); non-atomic RMW racing HotSpot's atomic _flags writers not stress-tested; set_dont_inline(nullptr)/invalid-pointer guard + silent-failure-log assertions are planned but the guards/logs are library fixes not yet pinned
- **make_unique** (80%, both): make_unique<T>("literal") -Werror=address/nonnull-compare (flaw #1) — library/build; no ExceptionClear after failed NewObjectA throwing <init> (flaw #2) — JVM, needs -Xcheck:jni; TLAB partial-object superclass-field-zero edge when construct() omitted (flaw #3); construct() arity/convertibility over-capture e.g. construct(long) for int (flaw #4)
- **make_java_array** (80%, both): object-array allocation on JDK8 only best-effort/[INFO] (FIX-D ObjArrayKlass); 100000 GC-forcing large alloc dropped on Windows (in-detour GC faults off-suite-thread); -XX:-UseCompressedOops 8-byte narrow-oop element width unexercised; 32-bit / >32GB-heap header layout variant untested; reference-array TLAB-miss path has no JNI fallback (by design) - uncharacterized failure mode
- **global_ref** (80%, both): survive-GC (phase 2: forced System.gc + post-GC read/reset) compiled out entirely on Windows; post-GC value/identity gated on resolve_oop_guarded attainability - degrades to [INFO] on G1 linux-gcc-java11; no double-DeleteGlobalRef live verification on relocated pin (left held, released at static dtor); NewGlobalRef table exhaustion / many-pins stress untested
- **find_class_fallback** (80%, both): JNI fallback chain (context-loader -> system -> Forge LaunchWrapper) catalogued correctness gaps only characterized, not all fixed; deeper ClassLoaderDataGraph walk edge cases (unloaded/redefined classes) need raw-VM-walk live assertions; stale-klass cache-eviction-on-class-unload path
- **classloader_reanchor** (80%, both): live multi-name reanchor end-to-end (override then every find_class follows) needs deeper live-JVM verification beyond the single anchorTick scoped_hook; reanchor under concurrent find_class readers; evict-then-reresolve race under klass_lookup_cache_mutex live
- **poly_inherited_oop** (80%, JVM): protectedAdd native call best-effort - skipped when _call_stub_entry absent (JDK21+); only a single 2-level A<-B hierarchy; deeper (3+ level) inherited-field super walk untested; inherited field shadowing (sub redeclares same-name field) not exercised; deeper inherited-instance raw-walk assertions #38-capped
- **dont_inline_dont_compile** (82%, both): GC-churn flag-survival assertion is one of the #38-stall-prone modules (forced System.gc on the no-SEH cells); get_flags() readback on JDK 8 (u1) / JDK 21+ (u4) only characterized, not asserted against a true layout (library doesn't guarantee it); _dont_inline silent no-op when Method._flags VMStruct unresolved but NO_COMPILE applies (half-apply) not exercised live
- **field_string** (82%, JVM): JDK8 char[] (no-coder) backing GET/SET on an actual JDK8 host (the char[] arm is dead code on JDK9+; only structurally reached); coder==1 UTF-16 SET round-trip beyond the '?'-mojibake documentation; astral/surrogate-pair String field (vs the read_java_string_nul_astral no-JVM twin) on the live heap; string field length-cap boundary uses raised 16Mi ceiling not the stale 4096 referenced in spec — re-verify caps match current constant
- **method_overload_java_dispatch** (82%, JVM): no-match raw-bit type-confusion (flaw #1) only safely characterized, numeric result deliberately unpinned; non-String reference-return overload readback path untested (is_string false-negative); first-by-name HotSpot Symbol-ordering identity recorded [INFO] only; call_stub dispatch path never live on CI
- **for_each_instance** (82%, JVM): conservative-scan MISS rate under region-based (G1) / coloured-pointer (ZGC/Shenandoah) collectors — only default-heap CI, miss-count is [INFO] never HARD; concurrent GC moving/collecting between header-read and visitor-call (the conservative race) not stress-tested; no no-JVM/degraded contract test (unregistered/empty heap returns 0) outside the live module; large-heap unmapped-page safe_read robustness not exercised at scale (deep raw chunk-walk capped by no-SEH stall risk)
- **return_set_wrapper_null** (82%, JVM): narrow-vs-wide store_oop bug (object over genuinely-null slot) is CHARACTERIZED, not fixed; the allow-through wide-store path depends on a fix that is not yet in the library; cross-type wrong-klass injection deref is cancelled, never allowed through (UB) - cannot be hardened without a lib klass check; no compressed-oop heap >32GB variant of the narrow/wide decision exercised; no no-JVM lane: the wrapper/null branches require a live oop + interpreter slot (encode_oop_pointer), so no compile-time/pure-logic coverage exists
- **method_throwing_call_site** (83%, JVM): call_stub leaks-pending-exception flaw (flaw#1) only exercised when stub present (absent on CI; module's defensive clear is load-bearing but the bug itself isn't reproduced); no is_error/threw discriminator on value_t — throwing return indistinguishable from real 0, only [INFO]; J/D throwing-method 64-bit default-cell caveat untested (only (I) covered); throw across the call_stub thread-state round-trip not driven
- **interface_polymorphism** (83%, JVM): interface-chain default-method fallback best-effort/[INFO] when interface VMStructs unexported or arrays unsafe to read on some JDK/config; _transitive_interfaces cold-path os::safe_read is exactly the raw-VM-walk that #38 no-SEH cell caps for deepening; -XX:-UseCompressedOops decode path
- **method_call_string** (84%, both): call_stub path '?'-replacement + read_java_string cap contract only branch-asserted when stub present (never on CI JDKs); empty-vs-null variant-tag divergence between paths only recorded; -XX:-UseCompressedClassPointers 16-byte-header read_java_string offset (arr+12/+16) untested; modified-UTF-8 vs standard-UTF-8 divergence on astral/NUL only on call_stub path
- **method_static** (84%, both): static get_method missing JVM_ACC_STATIC filter (flaw#1) only detected via is_static, call() itself never gated/crash-tested; object usability through static wrapper hard only on call_stub (absent on CI); null-String is_void path-divergence asserted only on call_stub; is_static u4 fallback-to-static_field-false on missing _access_flags VMStruct untested
- **for_each_thread** (84%, JVM): Path-1 vs Path-2 (Threads::_thread_list vs ThreadsSMRSupport::_java_thread_list) which-path-ran not asserted per-JDK (8/9 vs 10+); Path-2 no-dedup [LOW] gap surfaced only by observation, not a forced duplicate (cannot corrupt live list); no no-JVM/degraded-VM contract test (returns-empty when no Threads VMStruct) for the enumeration itself; deep raw-VM thread-list-walk assertions on a large/exotic thread graph capped by no-SEH stall risk
- **enum_singleton** (84%, JVM): native method_proxy::call (brightness/apply) best-effort/[INFO] when call_stub_entry absent (JDK21+/25); -XX:-UseCompressedOops mis-decode path (feature only works compressed); JDK that exposes none of the _narrow_oop VMStruct rename triple -> clean FAIL, not separately tested; deeper raw-oop-walk distinctness assertions are #38-capped on no-SEH cell
- **field_introspection** (85%, both): FINAL/VOLATILE/SYNTHETIC modifier fields proven indistinguishable across more shapes (finality-blind only spot-checked); get_compressed_oop on a [[ nested-array and interface-typed ref field on the live heap; raw_address width-alignment asserted for every width class on every JDK (currently representative); JDK21+ FieldInfoStream vs <=17 _fields layout cross-checked explicitly for the introspection accessors
- **field_inherited** (85%, JVM): deeper-than-3-level hierarchy (4+ levels) walk depth; diamond/interface-default inherited field shapes; inherited array-typed field through the walk decoded element-wise; explicit JDK8-17 _fields vs JDK21+ FieldInfoStream layout assertion for the inherited walk path
- **field_arrays_object** (85%, JVM): multi-dimensional object array [[Ljava/lang/Object; element walk; object array under compressed-oops-OFF; Object[] holding a mix of array-typed and scalar-typed elements; interface[]-declared field with concrete-impl elements
- **method_return_types** (85%, both): non-String Object return decode only [INFO] (no published-OOP identity cross-check); breaks under -XX:-UseCompressedOops; read_java_string >4096 / empty / zero-length conflation not distinguished; read_java_string compressed-class-header offset assumption (arr+12/+16) untested under -XX:-UseCompressedClassPointers; astral/embedded-NUL return strings (standard- vs modified-UTF-8 path divergence) not exercised; call_stub decode path never live on CI
- **for_each_loaded_class** (85%, JVM): per-JDK graph-walk strategy not asserted to have taken the right path (JDK21+ ClassLoaderData::_klasses vs JDK8-17 Dictionary / SystemDictionary fallback); missed-loader detection only via known-class presence, not an exhaustive loader-set proof; no no-JVM/non-HotSpot degraded contract test (empty snapshot when no ClassLoaderDataGraph VMStruct); CDS on/off variance and a stripped/leaner VMStructs not exercised; deep CLD-graph raw-walk at scale capped by no-SEH stall risk
- **return_caller** (85%, both): raw saved-rbp deref is not fault-safe (flaw #1: is_valid_pointer doesn't check committed page) - the SIGSEGV-on-unmapped-page risk is not provoked; no monotonicity/cycle check on the rbp walk (flaw #2) - a self-referential saved-rbp duplicate run is not synthesized (can't safely); no architecture guard for the -24 / [rbp+0] layout (flaw #3) - only x64 exercised; aarch64 silently-wrong path untested; caller() class-name walk lacks is_valid_pointer gating that stack_trace has (flaw #4) - divergence not stress-tested; deeper live raw-VM-walk hardening assertions capped by #38 no-SEH stall
- **nested_classes** (85%, JVM): JDK8 anonymous/local enumeration quirk only [INFO]-characterized; JDK exporting neither _fieldinfo_stream nor _fields -> all-nullopt degrade, untested; -XX:-UseCompressedOops this$0 ref-decode wrong (out of test config); deeper UNSIGNED5 _fieldinfo_stream raw-walk assertions on JDK21+ are #38-capped
- **method_call_return_void** (86%, both): is_void conflating returned-void vs dispatch-failure not surfaced as a defect test (no is_error); <init>/<clinit> CallNonvirtualVoid slot-93 reroute not exercised; call_stub-vs-call_jni static-discriminator divergence (object!=null && static_field) only reasoned, not driven; String-arg encoder divergence (make_java_string vs NewStringUTF) sidestepped with pure-ASCII, never exercised non-ASCII here
- **method_enumeration** (86%, both): get_methods_count unbounded reserve() DoS (flaw#1) not driven (would need corrupt _length); count/base TOCTOU under RedefineClasses race untested; hardcoded +8 Array data offset only implicitly via count==pairs; modified-UTF-8 / Unicode method-name decode not exercised (ASCII only); ()V upper bound left unconstrained (JDK8 synthetic delta)
- **return_stack_trace_depth** (86%, both): shares caller()'s non-fault-safe raw saved-rbp deref + missing monotonicity/cycle guard + no arch guard (same library flaws, capped by #38); the const_method::get_name unbounded ConstantPool index read (AV source on mingw+JDK24) is mitigated by guard-depth, not asserted/hardened; exact-index per-frame naming demoted to [INFO] (JDK-layout fragile), so position correctness is best-effort not HARD; deeper live raw-VM-walk assertions capped by #38 no-SEH safepoint stall
- **instanceklass_methods_walk** (87%, both): hardcoded Array<Method*> +8 offset / +0 length still raw ABI assumption (no VMStruct, no static_assert) — only implicitly via count==pairs; is_valid_pointer sentinel can drop a legit Method* at 0x..CAFEBABE (false-negative, ASLR-nondeterministic) unprovable; 11 inline clones of the walk vs canonical collector pinned together only behaviorally (one hook fire), not pointer-identity across all clones; long-name/Unicode-name symbol-decode boundary (under 0x1000 clamp) not exercised live
- **return_frame_raw_access** (87%, both): out-of-range over-read VALUE is only [INFO] (aliases live operand stack) - no defined-value assertion possible; get_locals() unavailable on some JDKs path (returns null) is tolerated, not positively asserted across the JDK matrix; raw frame-walk hardening (the cold-fault source) bounded by #38 no-SEH; deeper raw-slot assertions on fabricated/edge frames risk the no-SEH stall; no astral/compressed-oop-heap-variant decode of slot-0 receiver beyond the single decode_oop_pointer convention
- **field_static** (88%, JVM): inherited-static mirror-vs-declaring-klass offset path (declaring_klass!=target_klass) exercised explicitly on the live JVM; static array-typed field (e.g. static [I) get/set; static ref field to an interface/Object-declared type; JDK layout-variance of the Class mirror static-slot offset asserted across versions
- **field_object_ref** (88%, JVM): ref field decode under compressed-oops-OFF (raw 8-byte oop) layout; array-of-references field decoded element-wise on the live heap (beyond the '[' single-decode reject); ref to a hidden/anonymous/lambda class; weak/soft/phantom reference object field decode
- **field_arrays_primitive** (88%, JVM): null array reference field (vs empty array) on the live primitive-array path; multi-dimensional primitive array [[I element walk; very-large (>INT-cap-adjacent) length corruption rejection on a live oop; compressed-oops-OFF array layout
- **method_call_primitives** (88%, both): call_stub fast-path primitive decode hard-asserted (CI JDKs lack _call_stub_entry so only JNI path runs; stub path recorded-only); the 8 null-JNI-slot silent-monostate branches not driven on a live corrupt table; no error/throw signal distinguishing failed ()I from real 0 on either path
- **field_set_size_guard** (90%, both): anti-clobber strong assertion when a JDK lays the trio non-adjacently (degrades to [INFO]; not deterministically forced); reference-field width-guard parity (ref writes have no size guard) exercised on the live JVM; guard behaviour on a static field's mirror slot adjacency (anti-clobber is instance-layout-centric)
- **method_call_wide_args** (90%, both): call_stub size_of_parameters=param_idx wide-slot accounting (the [medium] flaw) unprovable on CI (stub absent); only pinned, would fire if a JDK re-exposed the stub; no live two-slot proof on the call_stub path itself

## Full ledger by cluster

### hooking-lifecycle

| cov% | lane | #38 | feature |
|---:|---|:---:|---|
| 38 | both | Y | seh_invoke_detour |
| 40 | no-JVM | - | hw_breakpoint_dr7 |
| 45 | both | Y | hook_common_detour_dispatch |
| 50 | both | Y | midi2i_trampoline_alloc |
| 68 | both | Y | interpreter_frame_walk |
| 70 | JVM | Y | method_entry_points_i2i_i2c |
| 72 | JVM | Y | deoptimize_methods |
| 75 | JVM | Y | hook_install_after_jit |
| 76 | both | Y | hook_verify_repair |
| 78 | JVM | - | hook_signature |
| 80 | both | - | hook_basic |
| 80 | JVM | - | hook_chaining |
| 82 | both | Y | dont_inline_dont_compile |
| 84 | JVM | - | hook_unhook_double_free |
| 85 | JVM | - | watch_static_field |
| 86 | JVM | - | shutdown_hooks_teardown |
| 88 | JVM | - | hook_reinstall_after_shutdown |

### fields

| cov% | lane | #38 | feature |
|---:|---|:---:|---|
| 82 | JVM | Y | field_string |
| 85 | both | Y | field_introspection |
| 85 | JVM | Y | field_inherited |
| 85 | JVM | Y | field_arrays_object |
| 88 | JVM | Y | field_static |
| 88 | JVM | Y | field_object_ref |
| 88 | JVM | Y | field_arrays_primitive |
| 90 | both | - | field_null_safety |
| 90 | both | Y | field_set_size_guard |
| 92 | JVM | - | field_primitives_get |
| 92 | JVM | - | field_primitives_set |
| 95 | no-JVM | - | field_proxy_value_t |
| 95 | no-JVM | - | field_proxy_set_guards |

### methods-calls

| cov% | lane | #38 | feature |
|---:|---|:---:|---|
| 72 | both | Y | method_call_object |
| 78 | both | Y | method_overload |
| 80 | JVM | Y | method_call_jni_fallback |
| 80 | JVM | Y | method_static_portability |
| 80 | both | Y | method_flags_width |
| 82 | JVM | Y | method_overload_java_dispatch |
| 83 | JVM | Y | method_throwing_call_site |
| 84 | both | Y | method_call_string |
| 84 | both | Y | method_static |
| 85 | both | Y | method_return_types |
| 86 | both | Y | method_call_return_void |
| 86 | both | Y | method_enumeration |
| 87 | both | Y | instanceklass_methods_walk |
| 88 | both | Y | method_call_primitives |
| 88 | both | - | method_explicit_signature |
| 90 | both | Y | method_call_wide_args |
| 90 | both | - | find_methods_by_signature |
| 93 | both | - | method_is_reference |
| 95 | no-JVM | - | method_proxy_value_t |
| 95 | no-JVM | - | method_call_arg_cap |

### collections-enumeration

| cov% | lane | #38 | feature |
|---:|---|:---:|---|
| 82 | JVM | Y | for_each_instance |
| 84 | JVM | Y | for_each_thread |
| 85 | JVM | Y | for_each_loaded_class |
| 86 | both | - | collection_iteration_safety |
| 88 | both | - | collection_list |
| 89 | both | - | collection_map |
| 90 | both | - | collection_linked_list |
| 90 | both | - | collection_set |
| 90 | both | - | collection_hash_tree_map |
| 92 | no-JVM | - | collection_type_tags |

### return-value

| cov% | lane | #38 | feature |
|---:|---|:---:|---|
| 82 | JVM | Y | return_set_wrapper_null |
| 85 | both | Y | return_caller |
| 86 | both | Y | return_stack_trace_depth |
| 87 | both | Y | return_frame_raw_access |
| 88 | both | - | return_set_arg |
| 90 | both | - | return_value_cancel |
| 93 | both | - | return_set_primitives |

### os-internals-codecs

| cov% | lane | #38 | feature |
|---:|---|:---:|---|
| 45 | both | Y | os_signal_handler |
| 48 | both | Y | constantpool_access |
| 50 | both | Y | vmstructs_offset_resolution |
| 55 | both | Y | decode_oop_and_pointers |
| 58 | no-JVM | - | os_protect |
| 60 | both | Y | compressed_klass_decode |
| 62 | no-JVM | - | os_allocate_release |
| 62 | both | Y | jni_arg_packing |
| 65 | both | Y | decode_u5_unsigned5 |
| 68 | both | Y | const_method_bounds |
| 70 | both | Y | compressed_oops_decode |
| 72 | no-JVM | - | os_query_region |
| 72 | no-JVM | - | logging_format |
| 75 | both | - | signature_parsing |
| 78 | both | Y | klass_introspection |
| 80 | no-JVM | - | os_safe_read |
| 80 | no-JVM | - | traits_function_traits |
| 80 | both | Y | make_unique |
| 82 | no-JVM | - | platform_capability_macros |
| 88 | no-JVM | - | version_macros |
| 90 | no-JVM | - | os_page_size_granularity |

### objectmodel-jni-classres

| cov% | lane | #38 | feature |
|---:|---|:---:|---|
| 8 | both | - | dllmain_bootstrap |
| 70 | no-JVM | - | object_factory |
| 72 | no-JVM | - | jni_forwarders |
| 78 | both | Y | make_java_string |
| 78 | JVM | Y | jni_local_ref_hygiene |
| 78 | both | Y | find_class_context_loader |
| 80 | both | Y | make_java_array |
| 80 | both | Y | global_ref |
| 80 | both | Y | find_class_fallback |
| 80 | both | Y | classloader_reanchor |
| 80 | JVM | Y | poly_inherited_oop |
| 82 | both | - | register_class |
| 82 | no-JVM | - | api_surface_no_jvm |
| 83 | JVM | Y | interface_polymorphism |
| 84 | JVM | Y | enum_singleton |
| 85 | both | - | wrapper_pattern |
| 85 | JVM | Y | nested_classes |
| 86 | no-JVM | - | unified_call_syntax |
