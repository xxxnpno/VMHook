schema_version: 1
slug: field_null_safety
title: Field Null Safety
category: field
status: in_progress
risk: low
specialist: .claude/agents/field_null_safety-specialist.md
description: |
  Read-side / lookup-side robustness of the field surface under degenerate inputs:
  `field_proxy::get()` / `set()` on a NULL field_pointer, `object_base` accessors
  on absent/empty/garbage field names, wrappers built from NULL oops, and deliberately-wrong
  signatures over real field pointers. The absolute contract is: never crash, always
  return the documented fallback (int32 zero for null field_proxy::get; std::nullopt for
  absent lookups; guarded no-op for null field_proxy::set), and never corrupt valid state.
java_versions:
- 8
- 11
- 17
- 21
- 24
- 25
- 26
tags: [field, null-safety, robustness, lookup, degenerate-input]
hpp_anchors:
  - symbol: "field_proxy::get()"
    file: vmhook/ext/vmhook/vmhook.hpp
    lines: [15217, 15350]
    note: "null-pointer guard at 15239-15241 (returns int32_t{} + signature); valid-pointer gate + sub-word alignment carve-out at 15272-15277"
  - symbol: "field_proxy::set()"
    file: vmhook/ext/vmhook/vmhook.hpp
    lines: [15366, 15600]
    note: "trivially-copyable branch guards at 15380; unique_ptr branch at ~15465; string branch routed through get_compressed_oop (returns 0 when !field_pointer)"
  - symbol: "find_field()"
    file: vmhook/ext/vmhook/vmhook.hpp
    lines: [13763, 13812]
    note: "null-klass check at 13766-13770; returns std::nullopt for absent names; caches only FOUND entries at 13805"
  - symbol: "object_base::get_field(const std::string_view name)"
    file: vmhook/ext/vmhook/vmhook.hpp
    lines: [17716, 17763]
    note: "instance-field null-oop guard at 17755-17759; static-field reads mirror independently at 17742-17752; routes through find_field which handles absent names"
  - symbol: "object_base::get_field(type_index, name) static"
    file: vmhook/ext/vmhook/vmhook.hpp
    lines: [17780, 17822]
    note: "static-only variant; does not require live OOP; reads mirror at 17811-17821; routes through find_field for absent-name safety"
  - symbol: "field_proxy value_t::signature / raw_address / is_static / is_reference / get_compressed_oop"
    file: vmhook/ext/vmhook/vmhook.hpp
    lines: [15199, 15216]
    note: "pointer-independent accessors that never deref field_pointer; well-defined on null proxy"
test_modules:
- tests/jvm/modules/field_null_safety.cpp
depends_on:
- field_introspection
- field_proxy_value_t
- find_class_fallback
related:
- field_inherited
- field_primitives_get
- field_static
audit_docs: []
known_bugs:
  - severity: low
    note: "Descriptor-blind null fallback (documented-by-design, pinned NOT fixed): field_proxy::get() on a null field_pointer returns int32_t{} alternative (variant index 3, value 0) for EVERY signature — null \"D\"/\"J\"/\"F\"/\"Ljava/lang/String;\" proxy reports int32 alternative, not double/long/float/reference. Caller that std::get the expected alternative by descriptor may be surprised. Severity: low (graceful, unintuitive). Recorded in lib_bugs. Line 15241, pinned across all 16 descriptors × both static/instance flags."
notes: |
  JDK-INVARIANT by construction: tests the library's own C++ guard logic (null checks,
  name-compare misses, mirror-vs-instance split), none of which depend on HotSpot field-layout
  differences across JDK 8/11/17/21/24/25/26. okInt storage is the Class mirror slot on every
  version; find_field walks InstanceKlass metadata uniformly. Every assertion is a hard ctx.check —
  there is nothing to JDK-gate. String backing array differences (Java 8 char[] vs Java 9+ byte[]/coder)
  are deliberately avoided; String checks only use as_string() (already JDK-version-handled by library)
  and null/empty-string paths (never touch backing array).
