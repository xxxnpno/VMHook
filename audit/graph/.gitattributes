# Force LF in the auto-generated vault so the CI drift check
# (`git diff --quiet -- audit/graph` after re-running the generator)
# stays stable across Windows / Linux contributors.
* text=auto eol=lf
